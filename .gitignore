/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Local agent/tool state
.claude/
.serena/

# Private planning corpus — roadmap / architecture / phase docs.
# Visible to me locally, never published to the repo.
.roadmap/

# Local end-to-end test fixtures — a seeded DB (+ vellum config) per engine for
# manual testing against `cargo install --path .`. Never published.
.bdd-test/
